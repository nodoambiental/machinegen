use super::types::TableTypes;
use super::util;

pub fn run(_sub_match: &clap::ArgMatches) {
    // Test table parsing
    // println!("{:?}", util::load_table(TableTypes::Replace));
    // println!("{:?}", util::load_table(TableTypes::Files));
    // println!("{:?}", util::load_table(TableTypes::Template));
}
